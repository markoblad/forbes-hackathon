<%= form_for(@forbes_user) do |f| %>
  <% if @forbes_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@forbes_user.errors.count, "error") %> prohibited this forbes_user from being saved:</h2>

      <ul>
      <% @forbes_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br />
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :linkedin %><br />
    <%= f.text_field :linkedin %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
